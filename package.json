{
  "name": "dentapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html app.js styles.css data.js manifest.json supabase-config.js documents dist/"
  }
}
